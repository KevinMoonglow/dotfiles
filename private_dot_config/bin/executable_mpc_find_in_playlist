#!/usr/bin/zsh

track="$(echo "$(mpc | pcregrep -o1 '^\[.*?]\s*#([0-9]+)/[0-9]+')" - 1 | bc)"

exec rofi -modi "mus:~/.config/bin/mpc_find_in_playlist_rofi" -show mus -selected-row "$track" -no-show-icons -window-title "Music Select" $*

#selection="$(~/.config/bin/formatted_mpc_playlist | rofi -dmenu -i -p "󰝚 Music" -format d -display-columns '1,2' -window-title "Music Select" -a "$track" -selected-row "$track" -no-show-icons -no-custom $*)"

#if test "$?" -eq 0; then
#    mpc play "$selection"
#fi
